<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    >
    <TextView
        android:id="@+id/contents"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/guideline"
        />
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.5"
        />
    <android.widget.Button
        android:id="@+id/check_button"
        android:padding="30dp"
        tools:text="네, 그렇습니다"
        android:autoSizeTextType="uniform"
        android:singleLine="false"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginVertical="30dp"
        android:textColor="@color/white"
        android:background="@drawable/accept_and_start_enabled_shape"
        app:layout_constraintTop_toTopOf="@id/guideline"
        app:layout_constraintBottom_toTopOf="@id/cancel_button"
        />
    <android.widget.Button
        android:id="@+id/cancel_button"
        android:padding="30dp"
        tools:text="아니요, 괜찮습니다."
        android:autoSizeTextType="uniform"
        android:singleLine="false"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:textColor="@color/white"
        android:background="@drawable/certificate_take_auth_number_disabled_shape"
        app:layout_constraintTop_toBottomOf="@id/check_button"
        app:layout_constraintBottom_toBottomOf="parent"
        />
</androidx.constraintlayout.widget.ConstraintLayout>